<Window x:Class="TyreDegradation.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}"
        MinHeight="600" MinWidth="450"
        Height="900" Width="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*" />
            <RowDefinition Height="60*" />
        </Grid.RowDefinitions>
        <materialDesign:Card Grid.Row="0" Padding="32" Margin="16">
            <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}">Selections</TextBlock>
        </materialDesign:Card>
        <materialDesign:Card Grid.Row="1" Padding="32" Margin="16">
            <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}">Results</TextBlock>
        </materialDesign:Card>
        <!-- <ContentControl Grid.Row="0" prism:RegionManager.RegionName="SelectionRegion" /> -->
        <!-- <ContentControl Grid.Row="1" prism:RegionManager.RegionName="ResultsRegion" /> -->
    </Grid>
</Window>